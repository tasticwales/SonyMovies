# SonyMovies
simple JSON API for a Movie Studio

stand alone application with no external requirments.

session is used to simulate database access

https://joshclose.github.io/CsvHelper/ supplied the CSV helper used within the project

The three endpoints are: 
POST /metadata 
GET /metadata/:movieId 
GET /movies/stats 

.net core c#
